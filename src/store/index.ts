// import { Provider } from 'react-redux'; // Import the Provider from react-redux
// import store from './store'; // Import the Redux store
// import { AppProps } from 'next/app';

// /**
//  * AppWithRedux Component
//  * Wraps the Next.js app with the Redux Provider for global state management.
//  *
//  * @param {AppProps} props - The app's props.
//  */
// const AppWithRedux: React.FC<AppProps> = ({ Component, pageProps }) => {
//   return (
//     <Provider store={store}>
//       <Component {...pageProps} />
//     </Provider>
//   );
// };

// export { store }; // Export the store for any non-component usages (like tests)
// export default AppWithRedux;
